{
  "name": "riftbound-duel",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["client", "server", "shared"],
  "scripts": {
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "dev": "npm run build -w shared && concurrently -n shared,server,client -c yellow,cyan,magenta \"npm run dev -w shared\" \"npm run dev -w server\" \"npm run dev -w client\"",
    "start": "npm run start -w server",
    "test": "npm run build -w shared && npm run test -w server",
    "typecheck": "npm run build -w shared && npm run typecheck -w server && npm run typecheck -w client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=24.14.1 <25"
  }
}
